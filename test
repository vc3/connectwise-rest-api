///<reference path="./typings/browser.d.ts"/>

'use strict';

import { Connectwise } from './src/index';
import * as util from 'util';

const params = { id: 583148 };

const cw: Connectwise = new Connectwise('connect.vc3.com', 'vc3', 'ic17OMw7uKkVVtPK', '5wEsGuAMfJph1U1k');

cw.TicketsApi.serviceTicketsIdGet(params).then( response => {
    console.log(util.inspect(response, {colors: true }));
});
